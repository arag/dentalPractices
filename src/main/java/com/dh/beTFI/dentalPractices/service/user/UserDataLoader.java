package com.dh.beTFI.dentalPractices.exception.service.user;

/*
import com.dh.beTFI.dentalPractices.model.User;
import com.dh.beTFI.dentalPractices.model.enums.UserRoleEnum;
import com.dh.beTFI.dentalPractices.repository.IUserRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.stereotype.Component;

@Component
public class UserDataLoader implements ApplicationRunner {
    @Autowired
    private IUserRepository userRepository;

    @Override
    public void run(ApplicationArguments args) throws Exception {
        BCryptPasswordEncoder passwordEncoder = new BCryptPasswordEncoder();
        String hash = passwordEncoder.encode("araceli");
        User user = new User("Ara", "araceli", "ara.g@gmail.com", hash, UserRoleEnum.ROLE_USER);
        userRepository.save(user);
    }
}
 */
